'use strict';

// Export config object directly (as below)
// -OR-
// via require('some-npm-module-that-exports-config')
module.exports = {
  MD043: {
    headers: [
      '# First',
      '## Second',
      '### Third'
    ]
  }
};
